# PyTorch is installed separately by setup_server_leanquant.sh so its CUDA
# runtime can be matched to the NVIDIA driver on the server.
transformers
datasets==3.6.0
tqdm
numpy
sentencepiece
accelerate
safetensors
lm-eval==0.4.4
wandb
python-dotenv
scikit-learn
